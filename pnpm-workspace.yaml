packages:
  - 'packages/*'
  - 'addons/*'

# Dependency overrides live in the root package.json under "pnpm.overrides".
# pnpm honours only one overrides source, and package.json is the active one —
# a duplicate block here was silently ignored, so it was removed to avoid the
# confusion of edits that never took effect.
